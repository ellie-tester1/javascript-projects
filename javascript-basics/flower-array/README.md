# Flower Array

This is a simple JavaScript project.

The program works with an array of flowers.

It changes the array depending on the number:

1 – add "Rose"  
2 – add "Carnation"  
3 – remove the last flower  
4 – remove the first flower  

The result is shown in the console.

Technologies:  
HTML, JavaScript
